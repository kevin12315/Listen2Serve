# 数据与素材许可说明（LICENSE_DATASET）

**SPDX-License-Identifier: Apache-2.0**

本仓库的**全部内容**——评测框架代码（`src/`、`scripts/`、`tests/`、`configs/`）与基准数据
（`data/benchmark/` 下的场景记录、子集清单、裁判量规、TTS 指令文本、合成音频样本，以及
`prompts/`、`docs/` 中的文本）——统一按根目录 [LICENSE](LICENSE) 的 **Apache License 2.0**
授权，版权主体为 Alibaba Group Holding Limited（见 `LICENSE` 附录与 [NOTICE](NOTICE)）。

**本文件不引入任何额外的许可条款。** 它只提供数据侧的来源事实与风险边界说明，帮助使用者在
自行合规评估时判断"这份数据能不能用、怎么用才稳妥"。Apache-2.0 允许商业使用、修改、再分发
以及把本基准用于模型训练，这一点不因下述说明而改变。

## 数据来源（provenance）

* **场景骨架**：派生自已脱敏的客服会话记录的骨架抽取结果，只保留角色、流程阶段与策略规则，
  **不包含原始对话原文**，也不包含可回溯到具体客户或坐席的标识符。
* **话术文本**：由 LLM 依据骨架重新生成，并经人工与规则校验，属于生成内容。
* **音频资产**：`data/benchmark/audio_samples/` 下全部样本均由商用 TTS 接口合成，
  **不含任何真人录音，也不含真人音色克隆**。真人声纹属个人敏感信息，本仓库不涉及。
* **实体信息**：场景中的姓名、公司名、金额、案情、岗位与补贴等均为虚构，如与现实重合属巧合。
* **指纹可核验**：`python scripts/check_dataset.py` 可校验素材指纹、平衡性与音频哈希，
  `data/benchmark/version.json` 记录 `dataset_version` 与切片标识。

## 使用提示（建议，不构成许可限制）

1. 再分发或发布衍生版本时，请保留 `LICENSE`、`NOTICE` 与本文件，并按 Apache-2.0 §4(b) 标注你所做的修改；
2. 请保留 `data/benchmark/version.json` 中的版本与素材指纹：跨数据集版本 / 裁判型号 / 提示词版本
   的评测数值不可直接比较，引用时请一并注明 commit 或 tag；
3. 请勿使用本仓库名称、徽标或其中提及的服务商品牌为衍生产品背书（Apache-2.0 §6 不授予任何商标权）；
4. 若把本基准用于对外产品或公开榜单，请自行确认你所调用的 TTS / ASR / LLM 服务端服务条款允许
   你将生成内容用于该用途——这一义务来自你与服务商之间的协议，与本许可无关；
5. 数据来源于生产业务骨架这一事实意味着：即便许可宽松，请在公开引用时避免暗示其对应任何真实
   企业、真实客户或真实通话记录。

## 关于"为什么数据也走 Apache-2.0"

本文件的前一版曾声明"仅限学术研究与评测、禁止商用与训练用途"（CC BY-NC 4.0 语义对齐）。
经评审决定把代码与数据统一到 Apache-2.0，使整个仓库以单一许可证申报与复用。需要明确的后果是：
非商业限制随之放弃，且**对已获取本版本的接收人不可追溯收回**——许可一旦分发即生效，后续改回
严格条款对既有接收人无效。如果某个使用场景确实需要非商业约束，应由该场景自己选择其他数据集。
