# DATA LICENSE（评测集数据许可说明）

`data/benchmark/` 下的评测集内容（场景脚本、音频资产、验证数据）是使用内部业务脱敏数据与 API 合成语音生成的衍生内容，发布时随仓库一并公开。

## 数据来源声明

- **场景骨架**：来源于内部客服会话数据（`an internal anonymised call-log export`）的脱敏骨架抽取（角色、流程阶段、策略规则），不包含原始对话原文；
- **话术文本**：由 LLM 根据骨架重新生成并经人工/规则校验，属于生成内容；
- **音频资产**：全部由 TTS API 合成，不包含任何真人录音。

## 使用限制

1. 本评测集仅用于学术研究与评测目的；
2. 禁止将评测集内容（含音频）用于训练生产模型或商业分发，除非另行获得授权；
3. 引用本评测集时请注明出处（详见 README 的引用信息）。

## 许可

- 代码部分：Apache-2.0（见根目录 LICENSE）；
- 评测集数据（`data/benchmark/`）：本文件所述的非商业学术使用许可（CC BY-NC 4.0 语义对齐，具体以最终发布为准）。
